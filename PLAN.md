# Daily Arcade Studio — 企画・実装プラン

最終更新: 2026-09-09

## コンセプト

**「毎日1本、遊べるブラウザゲームを出すチャンネル」**

毎日1本、テーマの違うブラウザゲームを公開し、
そのゲームの**世界観を物語として語る**動画をYouTubeに投稿する。

> **方針変更（2026-09-09）**: 「AIが作った」という切り口は使わない。
> 視聴者にとってはどう作ったかより**どんなゲームか**が重要。
> 制作背景は毎回「そのゲームが生まれた理由」の物語として創作する
> （史実である必要はない。ぶっ飛んでいてよい）。

- ゲームが**資産として残る**（動画は流れるが、遊べるURLは残り続ける）
- 全部Macローカル・完全無料で生成できる
- 既存3チャンネル（MosukeStudio / The Japan Puzzle / Algo-Gym）と同じ量産の型を使う

### ゲームの必須要件（重要）

視聴者の大半はスマホから概要欄のリンクを開く。**キーボード必須のゲームは
流入のほぼ全員が遊べない**ので、以下は必須とする（`lib/make_game.py` の
プロンプトと検証に組み込み済み）。

- **タッチ操作**: スワイプ／タップでキー操作と同じことが全部できる
- **STARTボタンとタイトルは `touchend` でも反応させる**
  （touch では click が発火しないことがある）
- **ゲームオーバー後は「Tap to retry」**（`pointer:coarse` で判定）
- **縦画面・横画面の両対応**: 縦は 16:9 より狭いので、アスペクト比に応じて
  カメラのFOVを広げる。`resize` と `orientationchange` の両方で再フィット
- **HUD・タイトル・K.O. は `clamp()`** で small screen でも溢れさせない
- キーボードとタッチは**同じ関数**を通す（実装が乖離しないように）
- **音は WebAudio で合成する**（mp3を置かない＝単一HTMLを維持）。
  ゲームの状態に反応させる: 速度に応じてテンポが上がる、イベントでスティンガー、
  ゲームオーバーで音を絞る。ミュートボタン（`M`キーでも）を必ず付ける。
  **`?auto=1` と `?showcase=` は必ず無音**（録画に音が乗るため）。
  **音階・キー・音色・リズムはゲームごとに変える**（同じSNDをコピーすると
  全部同じ音になる）
- **一覧に戻るリンク**（`../../`）を左上に置く。視聴者は概要欄から1本の
  ゲームに直接来るので、他のゲームへの導線が必要

### 動画の作り方（重要）

- **スライドに文字を並べない**。ゲーム内のキャラ・物体を実際に映しながら話す
  （`?showcase=` モード）。文字だけのスライドは離脱の原因
- **動画内にURLを出さない**。「概要欄のリンクから」と言う
  （URLを変えるときに動画を作り直さずに済む）
- 最後は**チャンネル登録・高評価のお願い**で締める
- 技術選定（なぜThree.jsか等）の話はしない。**ゲームの設計**の話をする
- 面白い部分だけ見せる。コードも「へえ」と思う部分だけ

## 技術スタック（全部フリー）

| 役割 | 使うもの | 状態 |
|---|---|---|
| ゲーム本体 | **Three.js**（単一HTML・CDN読み込み） | ✅ 動作確認済 |
| 3Dキャラ | 現状: Three.jsプリミティブ / 将来: **Blender MCP → .glb** | ✅ / ⏸ Blender不調 |
| ホスティング | **GitHub Pages**（DailyArcadeStudio/DailyArcadeStudio） | ✅ 公開中 |
| プレイ録画 | **Playwright**（chromium headless・autoplay AI操作） | ✅ 動作確認済 |
| ナレーション | **Kokoro TTS**（am_adam・英語） | ✅ 動作確認済 |
| 動画合成 | **ffmpeg** | ✅ 動作確認済 |
| スライド/字幕 | **PIL**（Algo-Gym資産を流用・GitHub-darkテーマ） | ✅ |
| 物語の絵 | **SDXL**（MosukeStudioの gen_images.py を流用） | ✅ |
| ショート | **ffmpeg**（16:9を縦にレターボックス） | ✅ |

## 第1作: Ninja Dash

3Dエンドレスランナー。忍者が鳥居の並ぶ夜道を走り、手裏剣と岩を避ける。

- 操作: ← → / A D で移動、Space でジャンプ
- 3レーン制、走行速度が徐々に上昇
- `?auto=1` でAI自動プレイ（録画用）
- 現状: **遊べる状態で完成・GitHubにpush済み**

### 現在の実装状況
- [x] ゲーム本体（Three.js・単一HTML・286行）
- [x] 忍者キャラ（プリミティブ組み立て・目/額当て/帯）
- [x] 手裏剣（回転する4芒星）＋岩の障害物
- [x] スコア・当たり判定・ゲームオーバー・リトライ
- [x] 自動プレイAI（autoPilot: 前方の障害物を見て回避/ジャンプ）
- [x] カメラ追従・ヒーローライト・月・鳥居の柱（パララックス）
- [x] GitHubへpush（git@github.com:DailyArcadeStudio/DailyArcadeStudio.git）

---

## これからやること

### A. ゲームを「動画映え」させる ✅ 完了

**驚き・盛り上がりのイベント**（理不尽すぎなくてOK）
- [x] **BOSS SHURIKEN**: 巨大手裏剣がカメラ後方から追い抜き、画面を薙ぐように通過
- [x] **TRIPLE WALL**: 3レーンのうち2つを塞ぐ壁（1レーンだけ空く・ジャンプ不可）
- [x] **SPEED SURGE**: 加速地帯（速度+12・FOV 62→72・スピードライン）
- [x] **BLACKOUT**: 一瞬の暗転（1.9秒・ライトと背景を落として復帰）
- [x] イベント発生時の**バナー表示**（画面中央にスラム＋ネオン発光）

**ゲームオーバー演出**
- [x] スローモーション（timeScale を 0 へイージング）
- [x] 被弾ズーム（カメラ距離 7.5 → 3.6）
- [x] 赤フラッシュ＋赤ビネット＋強シェイク
- [x] **K.O.** の大きな表示＋FINAL SCORE

イベントはシャッフルキューで管理（同じ順番にならない）。
初回8秒後 → 以降はイベント終了+5〜9秒間隔。

### B. 動画を「ちゃんとした解説動画」にする ✅ 完了

現状は「プレイ動画＋ナレ」だけ。以下の構成に作り直す。

**本編（16:9・1〜2分）の構成**

| 時間 | 内容 | 素材 |
|---|---|---|
| 0:00 | **フック**: プレイのちょい見せ（一番派手な瞬間） | 録画 |
| 0:05 | タイトル「AIに3Dゲームを作らせた」 | スライド |
| 0:10 | **キャラ紹介**: 忍者くんのアップ、装備の説明 | ゲーム内カメラ or スライド |
| 0:20 | **設計スライド**: 構成図・技術選定の理由（Algo-Gym風） | PILスライド |
| 0:35 | **アピールポイント**: 単一HTML / ブラウザで動く / 全部ローカル生成 | PILスライド |
| 0:45 | **本編プレイ**: イベント演出・ゲームオーバーを含む | 録画 |
| 1:10 | 締め: 遊べるURL | スライド |

**ショート（縦・30〜60秒）**
- 一番派手な瞬間だけを縦で切り出し
- 上にポイント、下にゲーム名（既存3チャンネルのショート形式に合わせる）

**設計スライドに載せる内容（Algo-Gymの深掘りスタイルで）**
- なぜThree.jsか（Unityと比較して: WebGLビルドが重い / 自動化しにくい）
- なぜ単一HTMLか（CDN読み込み・ビルド不要・GitHub Pagesにそのまま置ける）
- ゲームループの構造（requestAnimationFrame → 更新 → 判定 → 描画）
- 障害物のリサイクル方式（後方に流れたら前方へ再配置＝生成しっぱなしにしない）
- 自動プレイAIの仕組み（前方14ユニットを見て、同レーンなら回避）

本編は **1920x1080 / 1分56秒** で生成済み: `output/ninja_dash_ep1.mp4`

構成（10シーン・ナレ長に合わせて各カットを自動伸縮＋0.4秒クロスフェード）:

| # | 種別 | 内容 |
|---|---|---|
| 0 | 録画 | フック（BOSS SHURIKENの瞬間） |
| 1 | スライド | タイトル「I Made an AI Build a 3D Game」 |
| 2 | 録画 | キャラ紹介（走行カット） |
| 3 | スライド | なぜThree.jsか（Unity比較） |
| 4 | スライド | ゲームループ（コード＋解説） |
| 5 | スライド | 障害物リサイクル方式 |
| 6 | 録画 | 4イベント本編 |
| 7 | スライド | 自動プレイAI（コード＋解説） |
| 8 | 録画 | 被弾〜K.O. |
| 9 | スライド | 遊べるURL |

### C. 公開まわり

- [x] **GitHub Pages 公開中**
      - 一覧: https://dailyarcadestudio.github.io/DailyArcadeStudio/
      - Ninja Dash: https://dailyarcadestudio.github.io/DailyArcadeStudio/games/ninja-dash/
      - **ゲームごとに `games/<名前>/` を切る**。将来ドメインを変えるときも
        動画は概要欄のURLを差し替えるだけで済む
- [ ] README（遊び方・技術構成）
- [ ] 動画説明欄に遊べるURLを入れる
- [ ] ショート（縦・30〜60秒）の切り出し

### D. 量産化（第2作以降）

- [ ] `make_game.py`: お題を渡すと claude CLI がゲームを生成 → 検証 → デプロイ
- [ ] お題リスト（ideas_games.md）＋ ledger.py で重複管理（既存の型を流用）
- [ ] 自動で録画 → スライド生成 → ナレ → 動画合成 → YouTube投稿予約
- [ ] daily_batch.sh に4つ目のチャンネルとして組み込み

---

## 既知の問題

- **Blender MCP**: `execute_blender_code` が応答しない（`get_scene_info` は通る）。
  原因調査中。当面はThree.jsプリミティブでキャラを作る。
- **gh CLI**: インストール済み・認証済み。Pages有効化はAPIから可能。

## ファイル

- `index.html` — ゲーム本体（これ1つで動く・約420行）
- `PLAN.md` — このファイル
- 録画スクリプト: `/tmp/record_game.py`
- ナレ生成: `/tmp/narr_game.py`
- 生成済み動画: `/tmp/ninja_dash_video.mp4`（38秒・旧構成）

### パイプライン（`lib/`）

Algo-Gymのスライド資産を流用。3ステップの型は既存チャンネルと同じ:

```
python lib/render_slides.py scenes.json work/slides   # .venv     PIL
python lib/narrate.py       scenes.json work          # .venv-kokoro
python lib/assemble.py      scenes.json work out.mp4  # .venv     ffmpeg
```

- `scenes.json` — 絵コンテ。`clip` キーがあるシーンは録画から切り出し、
  無いシーンはスライドを描く
- `lib/slides.py` — Algo-Gymからコピーし、ブランド名のみ変更。
  **太字直後に空文字が入って豆腐(□)になるバグ**と、
  **太字の直後に句読点が来ると空白が入るバグ**をこのコピーで修正済み
- 収録は `/tmp/capture_ninja.py`（イベントを狙って発火させて録る）

### 収録用デバッグフック（index.html 内）

動画の「一番派手な瞬間」を狙って撮るために、コンソールから叩ける:

- `window.__ev('boss'|'wall'|'surge'|'blackout')` — イベントを発火。
  **死亡中は false を返して発火しない**（K.O.画面の上にバナーが出た事故の対策）
- `window.__kill()` — その場で被弾させ、K.O.演出を再生
- `window.__alive()` — プレイ中かどうか。収録側はこれを見て復活を待つ
- `?hold=11000` — 自動リトライまでの待ち時間(ms)。K.O.を長く映したい収録用

Playwright から `page.evaluate("()=>window.__ev('boss')")` で呼べる。
運任せに長回しせず、狙ったカットだけ収録できる。

### 動作確認メモ

- Python環境: **`/Users/fukushimatakumi/develop/music-llm/.venv`** に playwright、
  `.venv-kokoro` に Kokoro TTS（システムの python3 には入っていない）
- headless Chromium では `--enable-unsafe-swiftshader` が必要（WebGLソフトウェアレンダリング）
- 60秒自動プレイでJSエラーなし・4イベント＋K.O.すべて発火を確認済み
